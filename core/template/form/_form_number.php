#if(showLabel):
	<label for="{{name}}">{{label}}</label>
#endif
<input type="number" name="{{name}}" id="{{name}}" class="{{class}}" min="{{min_number}}" max="{{max_number}}" value="{{value}}" placeholder="{{label}}" style="{{style}}" {{required}}>
