<div class="prevnextnavi">
	#if(prev):
		<a class="prev" title="{{prev|title}}" href="{{prev|url}}">&larr;</a>
	#endif

	#if(next):
		<a class="next" title="{{next|title}}" href="{{next|url}}">&rarr;</a>
	#endif
</div>